<!iterate through each post and display both the image and the caption>
<% @posts.each do |post|%>
	<%=image_tag post.image.url(:medium)%>	<!instead of showing just the path>
	<%=post.caption%>
<%end%>